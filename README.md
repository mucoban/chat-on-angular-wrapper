# ChatOnAngular - Wrapper

Insert `<script src="coa-wrapper.js"></script>` to your main page and replace `iframe`'s `src`  at `coa-wrapper.js` file,
with the url on which your chat-on-angular app is serving.
